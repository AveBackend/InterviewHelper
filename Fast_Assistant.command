#!/bin/bash

# Переходим в директорию скрипта
cd "$(dirname "$0")"

# Запускаем основной скрипт
./start_fast.sh

echo ""
echo "👋 Нажмите любую клавишу для выхода..."
read -n 1 